version: '3'

services: 
    
    redis:
        image: "redis:alpine"
        restart: always
    db:
        image: "postgres:alpine"
        ports: 
            - "5432:5432"
        environment: 
            POSTGRES_PASSWORD: "admin"
            POSTGRES_DB: "annotations-db"
        restart: always

